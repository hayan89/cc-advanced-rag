// cc-advanced-rag MCP server entry point.
// Full implementation is added in Step 8 of the implementation plan.
// For now, this placeholder allows `bun install` and `tsc --noEmit` to succeed.

export const VERSION = "0.1.0";

if (import.meta.main) {
  console.error("[cc-advanced-rag] server not yet implemented — see implementation plan Step 8");
  process.exit(1);
}
